# mysql_native_connector_generator

Gera o motor Eloquent (`_$ModelMysql`) a partir de `@MysqlTable` / `@MysqlColumn`.

```bash
cd example
dart run build_runner build --delete-conflicting-outputs
```
