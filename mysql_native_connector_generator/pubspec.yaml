name: mysql_native_connector_generator
description: Codegen Eloquent-style para mysql_native_connector (@MysqlTable).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  # Alinhar com freezed do plugin (analyzer 10 / source_gen 2).
  analyzer: ^8.1.1
  build: ^3.0.0
  source_gen: ^3.0.0
  mysql_native_connector:
    path: ../

dev_dependencies:
  build_runner: ^2.7.0
