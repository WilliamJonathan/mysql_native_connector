name: mysql_native_connector_generator
description: Codegen Eloquent-style para mysql_native_connector (@MysqlTable).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  # source_gen 3.1.x quebra com analyzer >= 8.4 (getInvocation removido).
  analyzer: 8.2.0
  build: ^3.0.0
  source_gen: ^3.0.0
  mysql_native_connector:
    path: ../

dev_dependencies:
  build_runner: ^2.7.0
